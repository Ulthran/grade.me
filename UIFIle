import Foundation
import SpriteKit
import UIKit

class _____ {
  /*
  * Declare your UI elements here
  * SKNodes like this: let nodeName = SKSpriteNode(*init*)
  * For the *init* you can use this link: https://developer.apple.com/library/ios/documentation/SpriteKit/Reference/SKSpriteNode_Ref/
  * That link also has pretty much every class reference so you can use it to look stuff up
  * This is just the menu scene so you only put the UI elements for this scene in this file. You can make a new file for a new scene
  */
  
  override func didMoveToView(view: SKView) {
    /*
    * Now you set up your scene
    * Stuff like this to set the font color for an SKLabelNode: nodeName.fontColor = UIColor.greenColor()
    * You can find all the methods for all the different nodes at that link
    * If you need any help with this let me know
    */
    
    /*
    * To add a UI element to the scene you do: self.view!.addSubview(uiButtonName)
    * To add an SKNode to the scene you do: self.addChild(nodeName)
    * If you have a UIButton and you want to add an action to it (what happens when it is clicked), you do this:
    * uiButtonName.addTarget(self, action: #selector(funcName), forControlEvents: UIControlEvents.TouchUpInside)
    * Then you have to add a function like this:
    * func funcName(sender: UIButton) {
      do stuff
    }
    * Let me know if you get stuck trying to do stuff
    */
  }
  
  override func willMoveFromView(view: SKView) {
    self.removeAllChildren()
  }
}
